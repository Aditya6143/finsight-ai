# RockyBot / FinSight — Environment Variables
# Copy this file to .env and fill in your values.
# .env is gitignored and must never be committed.

# Provider selection: "ollama" (default, local) | "openai" (Streamlit Cloud)
PROVIDER=openai

# Required only when PROVIDER=openai
OPENAI_API_KEY=
